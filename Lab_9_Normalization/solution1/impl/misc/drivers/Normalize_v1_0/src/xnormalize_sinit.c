// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xnormalize.h"

extern XNormalize_Config XNormalize_ConfigTable[];

XNormalize_Config *XNormalize_LookupConfig(u16 DeviceId) {
	XNormalize_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XNORMALIZE_NUM_INSTANCES; Index++) {
		if (XNormalize_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XNormalize_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XNormalize_Initialize(XNormalize *InstancePtr, u16 DeviceId) {
	XNormalize_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XNormalize_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XNormalize_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

